apiVersion: v1
kind: Service
metadata:
  name: {{ include "viti-mcp.fullname" . }}
  labels:
    {{- include "viti-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "viti-mcp.selectorLabels" . | nindent 4 }}
